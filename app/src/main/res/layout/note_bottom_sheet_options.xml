<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingLeft="24dp"
    android:paddingRight="24dp"
    android:paddingBottom="16dp"
    android:paddingTop="16dp"
    app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior"
    android:background="@android:color/transparent"> <!-- padding을 0으로 설정 -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/note_add_btn_line"
        android:orientation="vertical"> <!-- 경계선을 추가 -->

        <Button
            android:id="@+id/button_camera"
            android:layout_width="match_parent"
            android:layout_height="26dp"
            android:background="@android:color/transparent"
            android:text="카메라로 촬영하기"
            style="@style/mg_12"/>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/middle_gray" /> <!-- 구분선 추가 -->

        <Button
            android:id="@+id/button_gallery"
            android:layout_width="match_parent"
            android:layout_height="26dp"
            android:background="@android:color/transparent"
            style="@style/mg_12"
            android:text="앨범에서 선택하기" />
    </LinearLayout>

    <Button
        android:id="@+id/button_cancel"
        android:layout_width="match_parent"
        android:layout_height="26dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/note_add_btn"
        android:text="취소"
        style="@style/mg_12"
        android:textColor="@android:color/white" /> <!-- 텍스트 색상을 흰색으로 설정 -->
</LinearLayout>
